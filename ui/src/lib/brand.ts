/**
 * The product's name, in one place.
 *
 * The owner renames this from time to time, and a name scattered across an app
 * bar, a title tag and three headings is renamed in two of them. Everything
 * that says the product's name reads it from here; the one copy that cannot is
 * `index.html`'s `<title>`, which has no module system, and must be edited with
 * this constant.
 *
 * `flowdesk` is not this. That is the engine and the repository, and it stays
 * spelled that way wherever it names them.
 */
export const PRODUCT = 'Backcom Desk'
