# 2026-09-13-orb-ny: a close outside the New York opening range continues in that direction

**Registered:** 2026-09-13 00:58 (commit time; the times first typed here were estimates) — before any code or run
**Status:** decided → docs/decisions/2026-09-13-orb-ny.md Previously: implemented — `fd-strategy::orb`, 5 tests incl. no-future; in-sample defaults on xauusd:5m: 102 trades, PF 0.897 (a first look, not a result)
**Batch file:** `docs/hypotheses/2026-09-13-orb-ny.toml`

## Claim

The first 30–60 minutes after the COMEX open (08:20 New York) absorb the
overnight order flow and set the day's reference range. A five-minute close
outside that range, taken before midday, continues far enough to pay a stop at
the other side of the range: institutional flow arrives at the open, and a
break of the range it built is that flow winning rather than being absorbed.

## Falsifier

Fails the gate (PF ≥ 1.2, expectancy ≥ 0.05R, ≥ 30 trades) or sits below the
95th percentile of the matched null in-sample; or survives in-sample and fails
on the pre-registered out-of-sample market; or the direction null puts the
side of the break inside a coin flip (< 95th percentile).

## Base method

`NEW: orb` — opening-range breakout.

- Inputs: `rangeStart` (hhmm New York, 0820), `rangeMinutes` (60),
  `entryUntil` (hhmm, 1200), `entryBufferPips` (0), `stopMode` (0 = other side
  of the range, 1 = range midpoint), `riskReward` (1.5), `minRangePips` (5),
  `maxRangeAtr` (3, ATR period `atrPeriod` 14), `pipSize` (0.1).
- Range: high/low of the bars whose New York time is in
  `[rangeStart, rangeStart + rangeMinutes)` on the same New York day; only
  usable once the last of them has closed.
- Entry: the **first** bar of the day, after the range is complete and before
  `entryUntil`, whose close is above `rangeHigh + buffer` (long) or below
  `rangeLow − buffer` (short). One trade per day. Range narrower than
  `minRangePips` or wider than `maxRangeAtr × ATR` → no trade that day.
- Stop: the other side of the range (or its midpoint). Target:
  `riskReward × |entry − stop|` from the close that signalled. Fill is the
  engine's: next bar's open.
- Must not read anything at index > i; the range and the "first breakout"
  test only look back within the day.
- Grid: `riskReward` × `entryBufferPips` = 3 × 3 = 9 cells.

## Data

- In-sample: `xauusd:5m` (1.4 years, Vantage)
- Out-of-sample: `xauduka:5m` (four years, Dukascopy bid, resampled from
  its minutes) — **chosen now, opened only after the in-sample result is
  recorded**
- Costs: `xauusd` config, spread $0.28/oz, swap-free; all hypotheses flat
  16:30–18:15 New York and weekdays only.

## Sample needed

One trade a day at most: ~350 opportunities in-sample, of which a break
happens on perhaps half. Four folds at nine cells need ≥ 60 out-of-sample
trades to say anything; fewer than that is "not enough", not "no".

## What each outcome means

- Survives in-sample AND out-of-sample AND direction null → decision record
  and a paper-run proposal.
- Survives in-sample only → closed; the record says the summer of 2025–26 on
  one feed is what it survived.
- Fails in-sample → closed. No wider grid, no second window.

## In-sample result (xauusd:5m, 4 folds, 200 matched nulls) — recorded 2026-09-13 before the out-of-sample run

```
hypothesis          trades  OOS PF  expect null p50 null p95   pct  verdict
orb/60m                 84   1.249   0.102    0.941    1.049  100%  SURVIVES
orb/30m                164   0.897  -0.073    0.941    1.049   27%  fail
orb/60m-expansion       48   1.284   0.109    0.960    1.244   96%  SURVIVES
```

Direction null, defaults, 102 trades, 1000 assignments: p50 1.001, p95 1.439;
actual 0.897 → **34th percentile, inside**. The side of the break is not
distinguishable from a coin flip on this sample even where the gated
walk-forward survives. Out-of-sample is opened because the protocol says
survivors get it; the direction null is already a mark against.

## Out-of-sample result (xauduka:5m, four years, pre-registered) — recorded 2026-09-13

```
hypothesis          trades  OOS PF  expect null p50 null p95   pct  verdict
orb/60m                189   0.945  -0.027    0.843    0.914  100%  fail: PF < 1.2; expectancy < 0.05R
orb/30m                394   1.088   0.052    0.843    0.914  100%  fail: PF < 1.2
orb/60m-expansion      153   1.115   0.047    0.903    1.000  100%  fail: PF < 1.2; expectancy < 0.05R
```

Direction null on the same market, defaults, 239 trades, 1000 assignments:
p50 0.930, p95 1.226; actual 0.943 → **54th percentile, inside.**

Every row is above its matched null and below the gate; the in-sample
ranking inverted (60m survived in-sample, 30m is the best out of sample).
The side of the break is a coin flip on both samples.

## Amendment, 2026-09-19: the in-sample 100% above is an ungated null, and one of the three numbers in circulation belongs to a different row

**What this file says.** The in-sample table prints:

```
orb/60m                 84   1.249   0.102    0.941    1.049  100%  SURVIVES
orb/60m-expansion       48   1.284   0.109    0.960    1.244   96%  SURVIVES
```

**What the decision says.** `docs/decisions/2026-09-13-orb-ny.md`:

```
                     in-sample                 out-of-sample
orb/60m              1.249  92% inside          0.945  72%
orb/60m-expansion    1.284  86% inside          1.115  96%
```

and the reason, which is the finding of that pass: *"the registered null
traded random bars across the whole day at ATR stops, and a method that trades
only the New York morning beats an all-day random entry on cost alone. Gated
to the same window the survivors are inside the noise."* The adversary put it
in one line: *"'100th percentile' measures session concentration vs all-hours
cost bleed, not breakout logic."*

**Which is right: the decision.** The 100% above is a true reading of the null
as this file registered it, and that null is the wrong instrument — it is not
gated to the hours the method trades. `2026-09-13-night-synthesis.md` records
the change as the first of the loop's process corrections: *"The null must
trade the same hours as the method (ORB pass: 100th percentiles that were
72nd–96th once gated)."* Both numbers are real; only the 92nd answers the
question this registration asked.

**And one correction to how the disagreement has been described.** Section 4
of `docs/research/VERDICTS.md` calls this "three numbers for one row, all in
the record" — 100%, 92nd and 86th. Checked against the decision's own table,
**86th is not this row**: it is `orb/60m-expansion`, a separate preset with 48
trades. `orb/60m` carries two numbers, not three — 100% against the
registered all-day null and 92nd against the window-gated one — and the pair
is a before-and-after, not a disagreement. The out-of-sample 72nd (60m) and
96th (expansion) are a third window again and belong to neither.

**What does not change.** Both presets failed out of sample at PF 0.95 and
1.12 against a gate of 1.2, the direction null is 34th in sample and 54th out
("a coin flip on both samples"), and `orb/60m-expansion`'s 48 in-sample trades
are under this file's own pre-registered floor of 60. The verdict does not
depend on which null is used.

*Written 2026-09-19 by the records session, from
`docs/decisions/2026-09-13-orb-ny.md` and `2026-09-13-night-synthesis.md`.
Nothing is recomputed. Raised as item 6 of `docs/research/VERDICTS.md`
section 4.*
